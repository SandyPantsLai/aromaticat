/**
 * Values merged into the MDX scope in `MdxBase` (see `features/docs/MdxBase.tsx`).
 * Add keys here only when MDX needs shared data without inline `export`.
 */
export const mdxScope: Record<string, unknown> = {}
